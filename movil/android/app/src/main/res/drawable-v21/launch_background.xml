<?xml version="1.0" encoding="utf-8"?>
<!-- Blanco liso, el mismo color con el que arranca el degradado de la pantalla
     de carga. Así el paso de la pantalla nativa a la de Flutter no da un salto
     de color. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white" />
</layer-list>
