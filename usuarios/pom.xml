<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>
	<groupId>com.backend</groupId>
	<artifactId>usuarios</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>usuarios</name>
	<description>Servicio de identidad: autenticacion, emision de JWT y gestion de usuarios</description>

	<properties>
		<java.version>21</java.version>
		<testcontainers.version>1.21.3</testcontainers.version>
		<!--
		     Version de la API de Docker con la que hablan las pruebas.

		     Docker Engine 29 retiro el soporte de las APIs anteriores a la 1.40.
		     Testcontainers 1.21.3 lleva DENTRO su propia copia sombreada de
		     docker-java-core (org.testcontainers.shaded...), y esa copia pide
		     /v1.32/info, que el motor contesta con un 400. Testcontainers lo lee
		     como "aqui no hay Docker" y TODAS las pruebas de integracion se
		     saltaban solas, en verde, sin probar nada.

		     No se arregla subiendo docker-java: al ir sombreado, la dependencia
		     externa no lo sustituye (comprobado con la 3.7.1). Testcontainers
		     1.21.4 trae la misma copia. La 2.x si lo corrige, pero reorganizo los
		     modulos y es otro cambio.

		     1.41 y no una mas alta a proposito: la copia sombreada solo conoce
		     hasta la 1.44, y el motor exige 1.40 como minimo. La 1.41 cae dentro
		     de esa ventana y ademas sigue valiendo en motores tan antiguos como
		     Docker 20.10, que es lo que puede haber en cualquier maquina o runner.
		-->
		<docker.api.version>1.41</docker.api.version>
		<jjwt.version>0.12.6</jjwt.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<!-- Avisos al solicitante cuando se resuelve su solicitud. Si no hay
		     credenciales de correo configuradas, el servicio arranca igual y
		     los avisos se registran en el log en vez de enviarse. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<!-- Valida los JWT que emite el propio servicio; evita reescribir un filtro a mano. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-resource-server</artifactId>
		</dependency>
		<!-- Inicio de sesion con Google y Facebook. En Boot 4 el starter pasa a
		     llamarse security-oauth2-client (antes oauth2-client). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security-oauth2-client</artifactId>
		</dependency>

		<!-- Unico servicio que EMITE tokens, por eso es el unico con jjwt. -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- En Spring Boot 4 la autoconfiguracion se reparte en modulos: con
		     flyway-core a secas las migraciones NO se ejecutan. El starter es
		     el que aporta spring-boot-flyway y engancha el DataSource. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Metricas de seguridad y rendimiento expuestas en /actuator/prometheus -->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>

		<!-- Expone /actuator/health. Sin este starter las propiedades
		     management.* no hacen nada y el endpoint devuelve 404. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<!-- Pruebas de integracion contra un PostgreSQL real en contenedor.
		     Verifican lo que las unitarias no pueden: migraciones Flyway,
		     consultas JPA y restricciones de la base. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Separacion de pruebas:
			       mvn test    -> Surefire, solo *Test (rapidas, sin Docker)
			       mvn verify  -> Failsafe, ademas *IT (integracion con Testcontainers)
			     Asi el ciclo corto no depende de tener Docker levantado. -->
			<!-- La version de la API va al JVM de las pruebas, no al del build:
			     la copia sombreada de docker-java la lee de las propiedades del
			     sistema al cargarse. Hace falta en los dos plugins porque el
			     sondeo de Docker tambien se evalua en las unitarias. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<systemPropertyVariables>
						<api.version>${docker.api.version}</api.version>
					</systemPropertyVariables>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<configuration>
					<systemPropertyVariables>
						<api.version>${docker.api.version}</api.version>
					</systemPropertyVariables>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
